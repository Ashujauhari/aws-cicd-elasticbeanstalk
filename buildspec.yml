version: 0.2
phases:
  pre_build:
    commands:
    - echo "Entered the pre-build phase"
  build:
    commands:
    - echo "Entered build phase"
    - mvn package
  post_build:
    commands:
    - echo "Entered the post-build phase"
    - mv target/Hello_SpringBoot_With_War-0.0.1-SNAPSHOT.war   Hello_SpringBoot_With_War-0.0.1-SNAPSHOT.war
artifacts:
  files:
    - Hello_SpringBoot_With_War-0.0.1-SNAPSHOT.war
    - .ebextensions/**/*